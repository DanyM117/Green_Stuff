@model Green_Stuff.Models.ViewModels.ExpositionToSunVM
@{
    ViewData["Title"] = "Detail";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (Model.oSun.IdSun == 0)
{
    <h1>Crear Exposicion al sol</h1>
}
else
{
    <h1>Editar</h1>
}

<div class="row">
    <div class="col-4">
        <form asp-action="Detail" asp-controller="ExpositionToSun" method="post">
            <input type="hidden" asp-for="oSun.IdSun" />
            <div class="mb-2">
                <label class="fore-label" asp-for="oSun.Name">Nombre</label>
                <input class="form-control form-control-sm" asp-for="oSun.Name" type="text" />
            </div>
            <div class="mb-2">
                <label class="fore-label" asp-for="oSun.Description">Descripción</label>
                <input class="form-control form-control-sm" asp-for="oSun.Description" type="text" />
            </div>
            <div class="mb-2">
                <label class="fore-label" asp-for="oSun.ModificationDate">Fecha modificación</label>
                <input class="form-control form-control-sm" asp-for="oSun.ModificationDate" type="text" />
            </div>
            <div class="mb-2">
                <div class="form-check form-switch">
                    <input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheckChecked" asp-for="oSun.Enabled">
                    <label class="form-check-label" for="flexSwitchCheckChecked" asp-for="oSun.Enabled">Habilitado</label>
                </div>
            </div>

            <div class="mb-3">
                @if (Model.oSun.IdSun == 0)
                {
                    <button class="btn btn-primary btn-sm w-25" type="submit">Crear</button>
                }
                else
                {
                    <button class="btn btn-primary btn-sm w-25" type="submit">Actualizar</button>
                }
                <a class="btn btn-dark btn-sm" asp-action="Index" asp-controller="ExpositionToSun">Volver</a>
            </div>
        </form>
    </div>

</div>