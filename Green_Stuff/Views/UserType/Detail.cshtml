@model Green_Stuff.Models.ViewModels.UserTypeVM
@{
    ViewData["Title"] = "Detail";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@if (Model.oUserType.IdUserType == 0)
{
    <h1>Crear rol</h1>
}
else
{
    <h1>Editar</h1>
}

<div class="row">
    <div class="col-4">
        <form asp-action="Detail" asp-controller="UserType" method="post">
            <input type="hidden" asp-for="oUserType.IdUserType" />
            <div class="mb-2">
                <label class="fore-label" asp-for="oUserType.Name">Nombre</label>
                <input class="form-control form-control-sm" asp-for="oUserType.Name" type="text" />
            </div>
            <div class="mb-2">
                <label class="fore-label" asp-for="oUserType.ModificationDate">Fecha modificación</label>
                <input class="form-control form-control-sm" asp-for="oUserType.ModificationDate" type="text" />
            </div>
            <div class="mb-2">
                <div class="form-check form-switch">
                    <input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheckChecked" asp-for="oUserType.Enabled">
                    <label class="form-check-label" for="flexSwitchCheckChecked" asp-for="oUserType.Enabled">Habilitado</label>
                </div>
            </div>

            <div class="mb-3">
                @if (Model.oUserType.IdUserType == 0)
                {
                    <button class="btn btn-primary btn-sm w-25" type="submit">Crear</button>
                }
                else
                {
                    <button class="btn btn-primary btn-sm w-25" type="submit">Actualizar</button>
                }
                <a class="btn btn-dark btn-sm" asp-action="Index" asp-controller="UserType">Volver</a>
            </div>
        </form>
    </div>

</div>