@model Green_Stuff.Models.ViewModels.CategoryVM
@{
    ViewData["Title"] = "Detail";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@if (Model.oCategory.IdCategory == 0)
{
    <h1>Crear categoría</h1>
}
else
{
    <h1>Editar</h1>
}

<div class="row">
    <div class="col-4">
        <form asp-action="Detail" asp-controller="Category" method="post">
            <input type="hidden" asp-for="oCategory.IdCategory" />
            <div class="mb-2">
                <label class="fore-label" asp-for="oCategory.Name">Nombre</label>
                <input class="form-control form-control-sm" asp-for="oCategory.Name" type="text" />
            </div>
            <div class="mb-2">
                <label class="fore-label" asp-for="oCategory.Description">Descripción</label>
                <input class="form-control form-control-sm" asp-for="oCategory.Description" type="text" />
            </div>
            <div class="mb-2">
                <label class="fore-label" asp-for="oCategory.ModificationDate">Fecha modificación</label>
                <input class="form-control form-control-sm" asp-for="oCategory.ModificationDate" type="text" />
            </div>
            <div class="mb-2">
                <div class="form-check form-switch">
                    <input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheckChecked" asp-for="oCategory.Enabled">
                    <label class="form-check-label" for="flexSwitchCheckChecked" asp-for="oCategory.Enabled">Habilitado</label>
                </div>
            </div>

            <div class="mb-3">
                @if (Model.oCategory.IdCategory == 0)
                {
                    <button class="btn btn-primary btn-sm w-25" type="submit">Crear</button>
                }
                else
                {
                    <button class="btn btn-primary btn-sm w-25" type="submit">Actualizar</button>
                }
                <a class="btn btn-dark btn-sm" asp-action="Index" asp-controller="Category">Volver</a>
            </div>
        </form>
    </div>

</div>
